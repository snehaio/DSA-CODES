package binarysearch;

public class sortedmatrixbs {
    public static void main(String [] args){

    }
    static int[] binarysearch(int [][] column, int row, int cstart, int cend, int target){ //this is for simple binary search in the column
        while(cstart<=cend){
            int mid =cstart +(cend-cstart)/2;
            if(column[row][mid] == target)
        }

    }
    static int[] search(int matrix[][], int target){
        int rows= matrix.length;
        int cols = matrix[0].length; //matrix can be empty
    }
}
